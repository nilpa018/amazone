{# templates/visitor/new.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}New Visitor{% endblock %}

{% block body %}
    <h1>Create new Visitor</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('app_visitor_index') }}">back to list</a>
{% endblock %}
